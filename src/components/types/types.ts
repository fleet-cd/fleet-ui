export enum Color {
    PRIMARY = "primary",
    DANGER = "danger",
    WHITE = "white",
    BLUE = "blue"
}

export enum Variant {
    STANDARD = "standard",
    CONTAINED = "contained",
    TEXT = "text"
}